#include "main.h"
/**
 * mul - Entry point
 * @a: The first number
 * @b: The second number
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int mul(int a, int b)
{
	return (a * b);

}
