#include "main.h"
/**
 * _abs - get the absolute of the number
 * @n: The character to get abs
 *
 * Return: On success 1.
 */
int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	else
		return (n);



}
